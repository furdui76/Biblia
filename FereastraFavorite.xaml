<Window x:Class="Biblia.FereastraFavorite"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adăugați la favorite"
        Height="350"
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">

    <StackPanel Margin="20">
        <TextBlock Text="Alege sau scrie numele listei:"
                   FontSize="14"
                   Margin="0,0,0,10"/>

        <ComboBox x:Name="comboListeFavorite"
                  IsEditable="True"
                  Height="30"
                  FontSize="14"
                  Margin="0,0,0,10"/>

        <!-- TextBox pentru comentariu -->
        <TextBlock Text="Comentariu (opțional):"
                   FontSize="14"
                   Margin="0,0,0,5"/>
        <TextBox x:Name="txtComentariu"
                 Height="60"
                 FontSize="14"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="80"
                    Margin="0,0,10,0"
                    Click="btnOK_Click"/>
            <Button Content="Cancel"
                    Width="80"
                    Click="btnCancel_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
